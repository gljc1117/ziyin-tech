# 医生负责任使用 AI：官网发布记录

- 发布授权：用户明确要求将本轮材料撰写为论文级文章并发布到子殷官网。
- 文章类型：专业述评／叙述性分析；不是期刊同行评审论文、临床指南或原始研究。
- 题名：医生如何负责任地使用人工智能：从六条实践原则到可追溯的临床协作。
- 路由：`/news/responsible-clinical-ai-six-rules-20260919`。
- 基线：`29fcf9a416e95065d1a44ae7bccf2b92c2a47296`；候选分支 `codex/responsible-clinical-ai-20260919`。
- 改动：新增文章与原创 SVG 框架图；将文章接入既有新闻目录。未改动其他文章正文、运行配置或数据服务。
- 正文含14个分节、37段文字、2张表、6项参考资料；文内引文可跳转文末对应参考条目。

## 内容核验

BMJ 仅获取出版方书目信息、原文节选及本轮提供的六项摘要；正文访问受限，未声称已取得全文。WHO 两项依据核验官方网页；DECIDE-AI 核验出版方摘要；CONSORT-AI 核验开放获取全文；个人信息保护法核验国家网信办转载的全文。

文章明确披露文献获取范围、非系统综述属性、生成式 AI 辅助及企业业务关联。具体流程和审计指标标为实践建议，不报告新增临床数据，不声称 MEDIFORGE 已完成全部功能、临床验证或监管许可。

## 发布前验证

- 最终候选 Next.js 16.2.1 生产构建（webpack）及 TypeScript 检查通过。
- 两个涉及的 TypeScript 文件 ESLint 通过；`git diff --check` 通过。
- 本地生产模式文章与配图 HTTP 200；37段正文全部匹配；6项引用链接及锚点匹配；2张表存在。
- 新闻列表与 sitemap 包含文章路由。
- SVG XML 校验通过。初次本地栅格预览缺少中文字体；使用 Noto 官方中文字体重新渲染后视觉检查通过。原始失败输出保留在任务执行记录中。
- 云端浏览器不能访问候选 localhost；本地 HTTP 核验在同一进程环境完成。实际官网的浏览器检查安排在自动部署后，不将本地检查冒充线上结果。

## 发布与恢复方式

沿用 GitHub main 关联的既有自动部署；发布时以基线为父提交，使用非强制引用更新，避免覆盖并行提交。线上发布完成以部署成功状态及正式域名文章回读为准。

如需撤回，可对本次内容提交生成独立 revert；不回退其他后续发布。候选目录及验证结果保留用于核查。
